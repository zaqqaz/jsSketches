Implement your own `customRepeat` function which should work the same like default string `repeat` function.

Exmaple
```
"Hello".repeat(2); // HelloHello

"Hello".customRepeat(2); // Shoud be same (HelloHello)
```
