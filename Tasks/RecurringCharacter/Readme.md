Write function which will return first recurring character in a given string
And null if string doesn't contains any recurring character.


Example:
```
"abcderd" -> d
"abcdgwl" -> null
"abab" -> a
```
